<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Emergency</title>
</head>
<body>
    <%- include('./partials/navbar') %>

    <div class="content">
        <h1 class="heading">EMERGENCY</h1>
        <div class="section-1">
            <p class="text">Nazar Hati, durghatna ghati. Emergencies can occur anytime and anywhere, so we have decided to make a rescue agency which will help people during emergencies. Our agency is availaible 24/7 on our website, you can press the button below to declare an emergency and the agency will reach out to you in a couple of minutes.</p>
            <img class="img" src="/twentyfour.png" alt="">
        </div>
        <% if (user.emergency) { %>
            <form action="/emergency/removeEmergency/<%= user.id %>" method="post">
                <p class="text2">EMERGENCY HAS BEEN DECLARED, SUPPORT WILL SHORTLY REACH OUT TO YOU.</p>
                <button class="go-link">REMOVE EMERGENCY</button>
            </form>
            <% } else { %>
                <form action="/emergency/<%= user.id %>" method="post">
                    <p class="text2">PRESS THE BUTTON BELOW ONLY IN CASE OF EMERGENCY.</p>
                    <button class="go-link">DECLARE EMERGENCY</button>
                </form>
            <% } %>
    </div>
</body>
<style>
    * {
        margin: 0;
        padding: 0;
    }
    body {
        /* height: 100vh; */
        width: 100%;
    }
    .content {
        width: 100%;
        height: 100%;
        display: flex;
        flex-direction: column;
    }
    .heading {
        font-family: 'cursive2';
        font-size: 5vw;
        text-align: center;
        color: #AD4569;
        margin-top: 4vw;
        /* margin-bottom: 2vw; */
        font-weight: 400;
    }
    .section-1 {
        width: 100%;
        height: 50%;
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: center;
    }
    .text {
        width: 40%;
        font-size: 2vw;
        color: #AD4569;
    }
    .text2 {
        font-size: 2vw;
    }
    .img {
        aspect-ratio: 1;
        width: 30vw;
    }
    form {
        margin-top: 2vw;
        margin-bottom: 2vw;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        align-self: center;
        gap: 2vw;
    }
    button {
        transition: all 1s ease;
        background: transparent;
        outline: none;
        border: 2px white solid;
        padding: 12px;
        cursor: pointer;
        font-size: 1.5vw;
        font-family: Outfit;
        border-radius: 15px;
        color: red;
    }
    button:hover {
        border-color: red;
    }
</style>
</html>